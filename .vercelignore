# Vercel은 프로젝트 뿌리의 api/를 서버리스 함수 자리로 삼는다.
# 우리 api/는 encore.moe에서 받아 둔 원본 JSON일 뿐이고 빌드에 쓰이지도 않는다
# (src/data/*.ts로 이미 옮겨 적어 두었다). 배포본에는 올리지 않는다.
api/

# 작업 기록 — 배포본에 필요 없다.
docs/

# 개인 저장 자료. 배포본은 브라우저에 저장하므로 쓸 일이 없다.
data/
